export const styles = (theme) => ({
  wrapper: {
    position: "relative",
    minHeight: "calc(100vh - 24px * 2 - 72px)",
  },
  fab: {
    position: "absolute",
    bottom: 643,
    right: 12,
  },
});
