import React, { Component } from "react";

class EmptyCart extends Component {
  render() {
    return (
      <section className="empty-cart section-title">
        <h1>your cart is currently empty</h1>
      </section>
    );
  }
}

export default EmptyCart;
